name: intelligent_wallet_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  fl_chart: ^1.1.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  tflite_flutter: ^0.11.0  # Still needed for fraud detection
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/wallet.jpg
    - assets/models/fraud_model.tflite
    # Removed: assets/models/inflation_model.tflite (using backend API instead)